<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Movie</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', sans-serif;
      background: #ffffff;
      color: #222;
    }

    .main-header {
      background: #ffffff;
      padding: 12px 0;
      position: sticky;
      top: 0;
      z-index: 1000;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
    }

    .main-header .container {
      max-width: 1200px;
      margin: auto;
      padding: 0 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo img {
      width: 130px;
      margin-top: 5px;
    }

    .nav-links {
      display: flex;
      gap: 25px;
    }

    .nav-links a {
      color: #444;
      text-decoration: none;
      font-weight: 500;
      font-size: 16px;
      transition: color 0.3s ease, border-bottom 0.3s ease;
      padding-bottom: 3px;
      margin-top: 3px;
    }

    .nav-links a:hover {
      color: #e31c25;
      border-bottom: 2px solid #e31c25;
    }

    h2.text-center {
      text-align: center;
      font-size: 28px;
      font-weight: 600;
      color: #000000;
    }

    .card {
      background: #ffffff;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.6);
      width: 100%;
      max-width: 550px;
      margin: 40px auto 60px;
      padding: 25px;
    }

    .image-preview {
      display: block;
      max-width: 220px;
      height: auto;
      margin: 0 auto 20px;
      border-radius: 8px;
      border: 2px solid #777777;
      padding: 5px;
      background: #f6f6f6;
    }

    .form-group {
      margin-bottom: 15px;
    }

    .form-group label {
      display: block;
      margin-bottom: 6px;
      font-weight: 500;
      color: #333;
    }

    .form-group input[type="text"],
    .form-group input[type="number"],
    .form-group input[type="file"],
    .form-group select {
      width: 96%;
      padding: 10px;
      border-radius: 6px;
      border: 1px solid #bbb;
      background: #fff;
      color: #000;
      font-size: 14px;
      transition: all 0.3s ease;
    }

    .form-group input:focus,
    .form-group select:focus {
      border-color: #e31c25;
      box-shadow: 0 0 5px rgba(40, 167, 69, 0.3);
      outline: none;
    }

    .btn-update {
      width: 100%;
      padding: 12px;
      background: #e31c25;
      color: #fff;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    .btn-update:hover {
      background: #e31c25;
      transform: translateY(-2px);
    }
  </style>
</head>
<body>
  <header class="main-header">
    <div class="container">
      <a href="/" class="logo">
        <img
          src="https://cdn.prod.website-files.com/600ee75084e3fe0e5731624c/65b6384089ec9e265952391f_bookmyshow-logo-vector-removebg-preview%20(1).png"
          alt="logo"
        />
      </a>
      <div class="nav-links">
        <a href="/">Home</a>
        <a href="/add">Add Movie</a>
      </div>
    </div>
  </header>

  <div class="container">
    <div class="card">
      <form action="/update-movie/<%= movie.id %>" method="post" enctype="multipart/form-data">
        <h2 class="text-center">Edit Movie</h2>

        <div class="form-group">
          <label>Title</label>
          <input type="text" name="title" value="<%= movie.title %>" required />
        </div>

        <div class="form-group">
          <label>Director</label>
          <input type="text" name="director" value="<%= movie.director %>" required />
        </div>

        <!-- ✅ Category Dropdown -->
        <div class="form-group">
          <label>Category</label>
          <select name="category" required>
            <option value="">-- Select Category --</option>
            <option value="Bollywood" <%= movie.category === 'Bollywood' ? 'selected' : '' %>>Bollywood</option>
            <option value="Hollywood" <%= movie.category === 'Hollywood' ? 'selected' : '' %>>Hollywood</option>
            <option value="Tollywood" <%= movie.category === 'Tollywood' ? 'selected' : '' %>>Tollywood</option>
            <option value="Gujarati" <%= movie.category === 'Gujarati' ? 'selected' : '' %>>Gujarati</option>
          </select>
        </div>

        <div class="form-group">
          <label>Description</label>
          <input type="text" name="description" value="<%= movie.description %>" required />
        </div>

        <div class="form-group">
          <label>Genre</label>
          <input type="text" name="genre" value="<%= movie.genre %>" required />
        </div>

        <div class="form-group">
          <label>Year</label>
          <input type="number" name="year" value="<%= movie.year %>" required />
        </div>

        <div class="form-group">
          <label>Upload New Image</label>
          <input type="file" name="image" />
          <% if (movie.image) { %>
            <img src="/<%= movie.image %>" class="image-preview" alt="<%= movie.title %>">
          <% } else { %>
            <img src="https://via.placeholder.com/200x300" class="image-preview" alt="No image">
          <% } %>
        </div>

        <button type="submit" class="btn-update">Update Movie</button>
      </form>
    </div>
  </div>
</body>
</html>
