<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Book</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", sans-serif;
    }

    body {
      background: #f2f4f8;
      color: #333;
    }

    /* ---------- HEADER ---------- */
    header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 18px 50px;
      background: #667eee;
      backdrop-filter: blur(15px);
      border-bottom: 1px solid rgba(255, 255, 255, 0.25);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      z-index: 100;
    }

    .logo {
      font-size: 30px;
      font-weight: 700;
      color: #ffffff;
      text-shadow: 0 3px 8px rgba(0, 0, 0, 0.2);
      letter-spacing: 1px;
      transition: transform 0.3s ease;
      text-decoration: none;
    }

    .logo:hover {
      transform: scale(1.05);
    }

    nav a {
      margin: 0 15px;
      text-decoration: none;
      color: #f9f9f9;
      font-weight: 500;
      padding: 8px 14px;
      border-radius: 8px;
      transition: 0.3s ease;
    }

    nav a:hover {
      background: rgba(255, 255, 255, 0.15);
      color: #ffe98a;
    }

    .add-btn {
      background: #ffffff;
      color: #2d68ff;
      padding: 10px 18px;
      border-radius: 30px;
      font-weight: 600;
      text-decoration: none;
      box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1);
      transition: 0.3s ease;
    }

    .add-btn:hover {
      background: #e9efff;
      transform: translateY(-2px);
      box-shadow: 0 6px 14px rgba(0, 0, 0, 0.15);
    }

    /* ---------- FORM ---------- */
    .form-container {
      max-width: 500px;
      margin: 140px auto 40px;
      background: #fff;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    }

    .form-container h2 {
      text-align: center;
      color: #4f86ed;
      margin-bottom: 25px;
    }

    .form-container table {
      width: 100%;
    }

    .form-container td {
      padding: 12px 0;
      font-size: 15px;
      color: #555;
    }

    .form-container input[type="text"],
    .form-container input[type="number"] {
      width: 100%;
      padding: 10px 12px;
      border: 1px solid #ccc;
      border-radius: 8px;
      outline: none;
      transition: 0.3s;
      font-size: 15px;
      color: #333;
      background-color: #fff;
    }

    .form-container select {
      width: 100%;
      padding: 10px 12px;
      padding-right: 40px;
      border: 1px solid #ccc;
      border-radius: 8px;
      outline: none;
      transition: 0.3s;
      font-size: 15px;
      color: #333;
      background-color: #fff;
      cursor: pointer;

      /* Only language field has dropdown arrow */
      background-image: url("data:image/svg+xml,%3Csvg width='14' height='10' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1l6 6 6-6' stroke='%234f86ed' stroke-width='2' fill='none'/%3E%3C/svg%3E");
      background-repeat: no-repeat;
      background-position: right 12px center;
      background-size: 14px 10px;

      appearance: none;
      -webkit-appearance: none;
      -moz-appearance: none;
    }

    .form-container input:focus,
    .form-container select:focus {
      border-color: #4f86ed;
      box-shadow: 0 0 0 3px rgba(79, 134, 237, 0.15);
    }

    .form-container input[type="submit"] {
      background: #4f86ed;
      color: #fff;
      padding: 12px 18px;
      border: none;
      border-radius: 8px;
      width: 100%;
      font-weight: 600;
      margin-top: 15px;
      cursor: pointer;
      transition: 0.3s;
    }

    .form-container input[type="submit"]:hover {
      background: #3a6fd1;
    }
  </style>
</head>

<body>

  <!-- HEADER -->
  <header>
    <a href="/" class="logo">📚 LibrarySphere</a>
    <nav>
      <a href="/">Home</a>
      <a href="#">Books</a>
      <a href="#">About</a>
      <a href="#">Contact</a>
      <a href="#">FAQ</a>
    </nav>
    <a href="/add-book" class="add-btn">+ Add Book</a>
  </header>

  <!-- EDIT FORM -->
  <div class="form-container">
    <h2>Edit Book Details</h2>
    <form action="/edit-book/<%= record.id %>" method="post">
      <table>
        <tr>
        <tr>
          <td>Image URL:</td>
          <td><input type="text" name="image" value="<%= record.image %>"></td>
        </tr>

        </tr>
        <tr>
          <td>Book ID:</td>
          <td><input type="text" name="id" value="<%= record.id %>" disabled></td>
        </tr>
        <tr>
          <td>Title:</td>
          <td><input type="text" name="title" value="<%= record.title %>"></td>
        </tr>
        <tr>
          <td>Author:</td>
          <td><input type="text" name="author" value="<%= record.author %>"></td>
        </tr>
        <tr>
          <td>Language:</td>
          <td>
            <select name="language">
              <option value="English" <%=record.language==="English" ? "selected" : "" %>>English</option>
              <option value="Hindi" <%=record.language==="Hindi" ? "selected" : "" %>>Hindi</option>
              <option value="Spanish" <%=record.language==="Spanish" ? "selected" : "" %>>Spanish</option>
            </select>
          </td>
        </tr>
        <tr>
          <td>Publication Year:</td>
          <td><input type="number" name="year" value="<%= record.year %>"></td>
        </tr>
        <tr>
          <td colspan="2"><input type="submit" value="Update Book"></td>
        </tr>
      </table>
    </form>
  </div>
</body>

</html>