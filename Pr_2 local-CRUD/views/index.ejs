<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Library Dashboard</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", sans-serif;
    }

    body {
      background: #f2f4f8;
      color: #333;
    }

    /* ---------- HEADER ---------- */
    header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 18px 50px;
      background: rgb(84, 80, 205);
      backdrop-filter: blur(15px);
      border-bottom: 1px solid rgba(255, 255, 255, 0.25);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      z-index: 100;
    }

    .logo {
      font-size: 30px;
      font-weight: 700;
      color: #ffffff;
      text-shadow: 0 3px 8px rgba(0, 0, 0, 0.2);
      letter-spacing: 1px;
      transition: transform 0.3s ease;
      text-decoration: none;
    }

    .logo:hover {
      transform: scale(1.05);
    }

    nav a {
      margin: 0 15px;
      text-decoration: none;
      color: #f9f9f9;
      font-weight: 500;
      padding: 8px 14px;
      border-radius: 8px;
      transition: 0.3s ease;
    }

    nav a:hover {
      background: rgba(255, 255, 255, 0.15);
      color: #ffe98a;
    }

    .add-btn {
      background: #ffffff;
      color: #2d68ff;
      padding: 10px 18px;
      border-radius: 30px;
      font-weight: 600;
      text-decoration: none;
      box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1);
      transition: 0.3s ease;
    }

    .add-btn:hover {
      background: #e9efff;
      transform: translateY(-2px);
      box-shadow: 0 6px 14px rgba(0, 0, 0, 0.15);
    }

    /* ---------- BANNER ---------- */
    .banner {
      height: 350px;
      background: linear-gradient(135deg, #4f86ed, #9b6df3);
      position: relative;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding-top: 100px;
      color: #fff;
    }

    .banner::before,
    .banner::after {
      content: "";
      position: absolute;
      width: 250px;
      height: 250px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.15);
      animation: float 6s ease-in-out infinite;
    }

    .banner::before {
      top: -50px;
      left: -80px;
    }

    .banner::after {
      bottom: -60px;
      right: -80px;
      animation-delay: 3s;
    }

    @keyframes float {

      0%,
      100% {
        transform: translateY(0) rotate(0deg);
      }

      50% {
        transform: translateY(-30px) rotate(15deg);
      }
    }

    .banner h1 {
      font-size: 50px;
      margin-bottom: 10px;
      text-shadow: 0 3px 6px rgba(0, 0, 0, 0.2);
    }

    .banner p {
      font-size: 20px;
      opacity: 0.9;
    }

    /* ---------- CARDS ---------- */
    .container {
      max-width: 1100px;
      margin: 60px auto;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 25px;
      padding: 0 20px;
    }

    .card {
      background: #fff;
      border-radius: 15px;
      overflow: hidden;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .card:hover {
      transform: translateY(-6px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
    }

    .card-body {
      padding: 20px;
      text-align: center;
    }

    .card-body h3 {
      margin-bottom: 8px;
      color: #4f86ed;
    }

    .card-body p {
      margin-bottom: 10px;
      font-size: 14px;
      color: #555;
    }

    .actions button {
      background: #4f86ed;
      border: none;
      color: #fff;
      padding: 6px 12px;
      border-radius: 6px;
      margin: 4px;
      cursor: pointer;
      transition: 0.3s;
    }

    .actions button:hover {
      background: #3a6fd1;
    }

    .card img {
      width: 100%;
      height: 250px;
      object-fit: contain;
      background: #fff;
      padding: 10px;
    }
  </style>
</head>

<body>

  <!-- HEADER -->
  <header>
    <a href="/" class="logo">📚 LibrarySphere</a>
    <nav>
      <a href="/">Home</a>
      <a href="#">Books</a>
      <a href="#">About</a>
      <a href="#">Contact</a>
      <a href="#">FAQ</a>
    </nav>
    <a href="/add-book" class="add-btn">+ Add Book</a>
  </header>


  <!-- BANNER -->
  <section class="banner">
    <h1>Welcome to LibrarySphere</h1>
    <p>Manage your library books easily</p>
  </section>

  <!-- CARDS -->
  <!-- CARDS -->
  <div class="container">
    <% books.forEach((book)=> { %>
      <div class="card">
        <!-- IMAGE AT TOP -->
        <img src="<%= book.image %>" alt="<%= book.title %>" class="card-img-top">

        <div class="card-body">
          <h3>
            <%= book.title %>
          </h3>
          <p><strong>ID:</strong>
            <%= book.id %>
          </p>
          <p><strong>Author:</strong>
            <%= book.author %>
          </p>
          <p><strong>Language:</strong>
            <%= book.language %>
          </p>
          <p><strong>Year:</strong>
            <%= book.year %>
          </p>

          <div class="actions">
            <a href="/edit-book/<%= book.id %>"><button>Edit</button></a>
            <a href="/delete-book/<%= book.id %>"><button>Delete</button></a>
          </div>
        </div>
      </div>
      <% }) %>
  </div>


</body>

</html>