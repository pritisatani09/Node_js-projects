<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>LibrarySphere - Home</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f4f6fc;
      margin: 0;
      padding: 0;
    }

    header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 18px 50px;
      background: rgb(84, 80, 205);
      z-index: 100;
    }

    .logo {
      font-size: 30px;
      font-weight: 700;
      color: #fff;
      text-decoration: none;
    }

    nav a {
      margin: 0 15px;
      text-decoration: none;
      color: #f9f9f9;
      font-weight: 500;
      padding: 8px 14px;
      border-radius: 8px;
      transition: 0.3s ease;
    }

    nav a:hover {
      background: rgba(255, 255, 255, 0.15);
      color: #ffe98a;
    }

    .add-btn {
      background: #fff;
      color: #2d68ff;
      padding: 10px 18px;
      border-radius: 30px;
      font-weight: 600;
      text-decoration: none;
    }

    .add-btn:hover {
      background: #e9efff;
      transform: translateY(-2px);
      box-shadow: 0 6px 14px rgba(0, 0, 0, 0.15);
    }

    .banner {
      height: 350px;
      background: linear-gradient(135deg, #4f86ed, #9b6df3);
      position: relative;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding-top: 100px;
      color: #fff;
    }

    .banner::before,
    .banner::after {
      content: "";
      position: absolute;
      width: 250px;
      height: 250px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.15);
      animation: float 6s ease-in-out infinite;
    }

    .banner::before {
      top: -50px;
      left: -80px;
    }

    .banner::after {
      bottom: -60px;
      right: -80px;
      animation-delay: 3s;
    }

    @keyframes float {

      0%,
      100% {
        transform: translateY(0) rotate(0deg);
      }

      50% {
        transform: translateY(-30px) rotate(15deg);
      }
    }

    .banner h1 {
      font-size: 50px;
      margin-bottom: 10px;
      text-shadow: 0 3px 6px rgba(0, 0, 0, 0.2);
    }

    .banner p {
      font-size: 20px;
      opacity: 0.9;
    }

    .card-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
      padding: 20px;
    }

    .card {
      border: 1px solid #ddd;
      border-radius: 8px;
      width: 250px;
      padding: 15px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      text-align: center;
      background-color: #EFEEE8;
      transition: transform 0.2s;
    }

    .card:hover {
      transform: scale(1.03);
    }

    .card img {
      width: 100%;
      height: 300px;
      object-fit: cover;
      border-radius: 6px;
      margin-bottom: 10px;
    }

    .card p {
      margin: 6px 0;
      font-size: 17px;
      color: #333;
    }

    .card button {
      margin: 5px;
      padding: 8px 12px;
      border: none;
      border-radius: 4px;
      background-color: #daa556;
      color: white;
      cursor: pointer;
      font-weight: 700;
      font-size: 15px;
      transition: all 0.2s ease-in-out;
    }

    .card button:hover {
      background-color: #777777;
    }

    .actions {
      margin-top: 10px;
    }
  </style>
</head>

<body>

  <header>
    <a href="/" class="logo">📚 LibrarySphere</a>
    <nav>
      <a href="/">Home</a>
      <a href="#">Books</a>
      <a href="#">About</a>
      <a href="#">Contact</a>
      <a href="#">FAQ</a>
    </nav>
    <a href="/add-book" class="add-btn">+ Add Book</a>
  </header>

  <section class="banner">
    <h1>Welcome to LibrarySphere</h1>
    <p>Manage your library books easily</p>
  </section>

  <div class="card-container">
    <% if (books.length> 0) { %>
      <% books.forEach((book)=> { %>
        <div class="card">
          <% if (book.image) { %>
            <img src="<%= book.image %>" alt="<%= book.title %>"
              style="width: 100%; height: 300px; object-fit: contain; border-radius: 6px; background-color: #fff;">

            <% } %>
              <h3>
                <%= book.title %>
              </h3>
              <p><b>Author:</b>
                <%= book.author %>
              </p>
              <p><b>Price:</b> ₹<%= book.price %>
              </p>
              <p><b>Category:</b>
                <%= book.category %>
              </p>
              <p><b>Publisher:</b>
                <%= book.publisher %>
              </p>
              <p><b>Year:</b>
                <%= book.publicationYear %>
              </p>
              <div class="actions">
                <a href="/edit-book/<%= book._id %>"><button>Edit</button></a>
                <a href="/delete-book/<%= book._id %>"><button>Delete</button></a>
              </div>
        </div>
        <% }) %>
          <% } else { %>
            <p>No books found.</p>
            <% } %>
  </div>

</body>

</html>